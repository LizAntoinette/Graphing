export function bfs(graph, startNode, endNode) {
  let prev = solve(graph);

  return constructPath(startNode, endNode, prev);
}

function solve(graph) {
  //let q =
}
function constructPath(startNode, endNode, prev) {}
